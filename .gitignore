# --- Input data lives on Zenodo; outputs are regenerated, not committed -----
data/
outputs/
programs/
predictors_enmtml/

# --- Built container images (rebuild from Dockerfile; never committed) ------
*.sif
*.tar
*.tar.gz

# --- HPC / run artefacts ----------------------------------------------------
logs/
*.out
*.err
slurm-*.out
docker_build.log
docker_build.err
apptainer_tmp/
apptainer_cache/

# --- R / session cruft ------------------------------------------------------
.Rhistory
.RData
.Rproj.user/
Rplots.pdf
